#Testing out an actions i created to build centos 7 rpm
#Make some changes to the entrypoint.sh, docker file based on the wf on test action branch then retry

name: ci

on: 
  push:
    branches: [feature/test-buildout]
         
jobs:
    centos7-rpm:
        runs-on: ubuntu-16.04
        steps:
          - name: c7 
            uses: blackone-sudo/build-rpm@v1
            with: 
              spec_file: "*.spec"

