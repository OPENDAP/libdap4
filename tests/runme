echo
for i in *T
do
    echo "**** running test $i"
    outfile="${i}.out"
    ${i} > ${outfile} 2>&1
    cat ${outfile} | grep FAIL
    isdone=`cat ${outfile} | grep done`
    if [ "${isdone}" = "" ]
    then
	echo "did not complete"
    else
	echo "completed successfully"
    fi
done

